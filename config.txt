# config.txt - This file will allow you to change how Ancile functions by 
#	setting environment variables.
# 	
# 	Any line starting with a "#" will be treated as a comment.

# IDSTRING (String) - Add a unique ID string to the beginning of each loggong
#	run. If you need to, you can now ID log files by adding an ID string.
#SET IDSTRING=%DATE% %TIME% - %USERDOMAIN%::%USERNAME%% - %OS% (%COMPUTERNAME%) 

# LOGFILE (Drive+Path+filename) - Set the log file path and file name
#	You need to include the drive letter, full path, and file name.
#	The deafult location is to log to the Ancile directory.
#SET LOGFILE=%USERPROFILE%\Documents\somethingthatdontexist\AncileLog.txt

# SYSTEMRESTORE (Value) - Configure behavioir of system restore
#	Y	- Automatically launch Windows system restore before making changes.
#	N	- Do not launch Windows system restore
#	A	- Ask the user what to do (DEFAULT).
SET SYSTEMRESTORE=A

# DOSYSTEMINFO (Boolean) - Add system information from the log file
#	Y	- Add system information to the log file (Default).
#	N	- Do not add system information to the log file.
SET DOSYSTEMINFO=Y

# DOTIMESYNC (Boolean) - Change Windows NTP settings
#	Y	- Change Windows NTP settings to use ntp.org (Default).
#	N	- Do not modify how Windows syncs time.
SET DOTIMESYNC=Y

# MODWUBEHAVIOR (Boolean) - Disable modification of windows update behavior
#	Y	- Swith Windows Update to check and notify but do not download (DEFAULT).
#	N	- Do not change Windows Update Behavior.
SET MODWUBEHAVIOR=Y

# DISABLEREMREG (Boolean) - Disable modification of remote registry settings.
#	Y	- Disable remote registry (DEFAULT).
#	N	- Do not change remote registry settings.
SET DISABLEREMREG=Y

# DISABLEIE (Boolean) - Disable automatic download of Internt Explorer
#	Y	- Disable Automatic download and upgrade of IE (DEFAULT).
#	N	- Do not change Automatic download and upgrade of IE.
SET DISABLEIE=Y